[package]
name = "core"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = {version = "4.6.1", features = ["derive"]}
to-do-dal = {path = "../dal"}

[features]
json-file-storage = ["to-do-dal/json-file"]
default = ["json-file-storage"]